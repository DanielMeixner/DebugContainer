apiVersion: v1
kind: Service
metadata:
    name: "dmxaksds"
    labels:
        app: "dmxaksds"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "dmxaksds"